extends layout

block content
  .w3-card-4
    header.w3-container.w3-red
      h2 Ocorreu um erro

    .w3-container.w3-padding
      if message
        p
          b Mensagem:
          |  #{message}
      if error
        pre= error.message

      br
      a.w3-btn.w3-grey(href="/filmes") Voltar
